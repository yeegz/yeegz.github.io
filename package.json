{
  "name": "yeegz-portfolio",
  "private": true,
  "scripts": {
    "serve": "python3 -m http.server 4287 --bind 127.0.0.1",
    "test": "playwright test",
    "test:approved": "playwright test tests/approved-portfolio.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
